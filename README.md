# Brave-Backgrounds
The backgrounds brave uses

We used the inspect console command 
```window.getComputedStyle(document.querySelector("#root > div > div"),'::before').backgroundImage.split("url(\"")[1].replace("\")","")```
